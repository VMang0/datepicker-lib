export enum ArrowDirection {
  LEFT = 'left',
  RIGHT = 'right',
}

export enum FirstWeekDay {
  MONDAY = 'Monday',
  SUNDAY = 'Sunday',
}

export enum CalendarMode {
  YEAR = 'year',
  MONTH = 'month',
  DAY = 'day',
}
